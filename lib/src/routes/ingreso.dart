import 'package:flutter/material.dart';

class IngresoScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pantalla de Ingreso'),
      ),
      body: Center(
        child: Text('Bienvenido a la pantalla de ingreso'),
      ),
    );
  }
}